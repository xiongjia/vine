---
title: Sheet
description: Slide-over panel for dialogs and mobile interfaces
---

import { Button } from "@vine/ui";
import {
  Sheet,
  SheetContent,
  SheetDescription,
  SheetFooter,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from "@vine/ui";

# Sheet

A slide-over panel that appears from the edge of the screen.

## Default (right side)

```tsx preview
<Sheet>
  <SheetTrigger asChild>
    <Button variant="outline">Open Sheet</Button>
  </SheetTrigger>
  <SheetContent>
    <SheetHeader>
      <SheetTitle>Edit Profile</SheetTitle>
      <SheetDescription>
        Make changes to your profile here. Click save when you're done.
      </SheetDescription>
    </SheetHeader>
    <div className="grid gap-4 py-4">
      <p className="text-sm text-muted-foreground">Sheet content goes here.</p>
    </div>
    <SheetFooter>
      <Button type="submit">Save changes</Button>
    </SheetFooter>
  </SheetContent>
</Sheet>
```

## Left side

```tsx preview
<Sheet>
  <SheetTrigger asChild>
    <Button variant="outline">Open from Left</Button>
  </SheetTrigger>
  <SheetContent side="left">
    <SheetHeader>
      <SheetTitle>Navigation</SheetTitle>
      <SheetDescription>Sheet slides in from the left side.</SheetDescription>
    </SheetHeader>
    <div className="py-4">
      <nav className="flex flex-col gap-2">
        <a href="#/overview" className="text-sm hover:text-primary">
          Overview
        </a>
        <a href="#/button" className="text-sm hover:text-primary">
          Button
        </a>
        <a href="#/card" className="text-sm hover:text-primary">
          Card
        </a>
      </nav>
    </div>
  </SheetContent>
</Sheet>
```

## API

| Component          | Description                     |
| ------------------ | ------------------------------- |
| `Sheet`            | Root sheet container            |
| `SheetTrigger`     | Button that opens the sheet     |
| `SheetContent`     | Sheet content panel             |
| `SheetHeader`      | Header with title + description |
| `SheetTitle`       | Sheet title                     |
| `SheetDescription` | Sheet description               |
| `SheetFooter`      | Footer action area              |
| `SheetClose`       | Close trigger                   |
